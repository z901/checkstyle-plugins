name: Remove merged up-* PRs
on:
  pull_request:
jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: Delete merged branch
        uses: koj-co/delete-merged-action@master
        with:
          branches: "up-*"
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
